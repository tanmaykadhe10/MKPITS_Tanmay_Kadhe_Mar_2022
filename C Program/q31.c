#include<stdio.h>
#include<conio.h>
void main()
{
	int a,b;
	printf("enter the two number :");
	scanf("%d %d",&a,&b);
	if (a!= b)
	{
		
		if(a,b)
		{
			printf("it  is assending order \n");
			
		}
		else
		{
			printf("it is descending order \n");
		}
	}
}
