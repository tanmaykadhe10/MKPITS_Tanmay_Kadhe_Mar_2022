#include<stdio.h>
#include<conio.h>
void main()
{
	int  r,l,s;
	float pi=3.142,sp  ;
	printf("enter  the value of radius  : " );
	scanf("%d",&r);
	printf("enter the value of length :");
	scanf("%d",&l);
	sp= 8*pi*r*l;
	printf("sphere area is = %f",sp);
}
